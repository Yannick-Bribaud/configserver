server:
  port: 7027

spring:
  application:
    name: users
  config:
    import: "optional:configserver:http://localhost:8000/"
  datasource:
    url: jdbc:mysql://localhost:3315/users-db
    username: root
    password: root
  jpa:
    database-platform: org.hibernate.dialect.MySQL8Dialect
    show-sql: true
    hibernate:
      ddl-auto: update


management:
  endpoints:
    web:
      exposure:
        include: "*"
  health:
    readiness-state:
      enabled: true
    liveness-state:
      enabled: true

  endpoint:
    shutdown:
      enabled: true
    health:
      probes:
        enabled: true
  info:
    env:
      enabled: true


endpoints:
  shutdown:
    enabled: true

eureka:
  instance:
    preferIpAddress: true
  client:
    fetchRegistry: true
    registerWithEureka: true
    serviceUrl:
      defaultZone: http://localhost:8015/eureka/

info:
  app:
    name: "users-ms"
    version: "1.0.0"
    description: "users microservice of Tegus"

logging:
  level:
    com:
      upsilon:
        users: DEBUG
    #OpenTelemetry config
  pattern:
    level: "%5p [${spring.application.name},%X{traceId},%X{spanId}]"
    console: "%green(%d{yyyy-MM-dd'T'HH:mm:ss.SSSXXX}) %blue(%-5level) %red([%thread]) %yellow(%logger{20}) - %msg%n"
